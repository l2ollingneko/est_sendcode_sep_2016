# Uncomment the next line to define a global platform for your project

platform :ios, '8.0'
use_frameworks!

target 'est' do
	
	# Pods for est
	pod 'Alamofire'
	pod 'SwiftyJSON'
	pod 'SwiftKeychainWrapper'

end
